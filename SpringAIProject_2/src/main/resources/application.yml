server:
  port: 9090
spring:
  
  thymeleaf:
    cache: false
    encoding: UTF-8
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML

  ai:
    google:
      genai:
        api-key: ${GEN_KEY}
        project-id: gen-lang-client-0122754073
        location: us-central1
        chat:
          model: gemini-3.6-flash